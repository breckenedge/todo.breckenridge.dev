<%= link_to new_project_path, class: 'button button-wide' do %>
  New Project
<% end %>

<br>

<div class="link-list">
  <% @projects.each do |project| %>
    <%= link_to project_path(project) do %>
      <%= complete_icon if project.complete? %>
      <%= project.name %>

      <% if project.due_date %>
        <span class="badge"><%= time_ago_in_words project.due_date %></span>
      <% end %>
    <% end %>
  <% end %>
</div>
