<%= simple_form_for(@project) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <%= f.input :name %>
  <%= f.input :due_date, html5: true %>
  <%= f.input :details %>
  <%= f.input :estimates_enabled, label: 'Enable estimates?' %>
  <%= f.input :status, as: :boolean, label: "Complete?", checked_value: "complete", unchecked_value: "incomplete" %>

  <%= f.button :submit, 'Save', class: 'button button-wide accent5' %>
<% end %>

<hr>

<% unless @project.new_record? %>
  <%= button_to 'Delete Project', project_path(@project), class: 'button button-wide accent0', method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
