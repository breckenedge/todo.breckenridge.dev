<%= link_to 'New Category', new_todo_category_path, class: 'btn btn-primary btn-block' %>

<br>

<div class="link-list">
  <% @todo_categories.each do |category| %>
    <%= link_to todo_category_path(category), class: 'todo-category' do %>
      <%= category.name %>
      <%= todo_category_todos_count(category) %>
    <% end %>
  <% end %>
</div>
