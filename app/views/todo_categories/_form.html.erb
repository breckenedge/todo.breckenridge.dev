<%= simple_form_for(todo_category) do |form| %>
  <% if todo_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(todo_category.errors.count, "error") %> prohibited this todo_category from being saved:</h2>

      <ul>
        <% todo_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.input :name, autofocus: true %>

  <div class="two-column">
    <%= form.submit class: 'button' %>
    <% unless @todo_category.new_record? %>
      <%= link_to 'Delete Category', todo_category_path(@todo_category), class: 'button button-red', data: { method: :delete, confirm: 'Are you sure?' } %>
    <% end %>
  </div>
<% end %>
